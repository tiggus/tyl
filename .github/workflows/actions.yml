name: tyl

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Terraform init
        run: terraform init

      - name: Terraform plan
        run: echo plan

      - name: Terraform apply
        run: echo apply